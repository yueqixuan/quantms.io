name: qpx
channels:
  - conda-forge
  - bioconda
dependencies:
  # Core
  - python>=3.10
  - click>=8.1.7
  - pandas>=2.1.3
  - numpy>=1.24.3
  - pyarrow>=14.0.1
  - pyopenms>=3.0.0
  - duckdb>=1.2
  # Optional (uncomment as needed)
  # - biopython     # transforms: gene mapping
  # - mygene        # transforms: gene mapping via MyGene.info API
  # - anndata       # transforms: absolute/differential expression
  # - plotly        # plotting: interactive charts
